It's not key sensitive, but it's better to keep using function with uppercase;

ACID:
- atomicity:
- consistency:
- isolation:
- durability:

*OBS: Limit to 300 rows (a bit more): it's going to turn the query more efficient

---------- SQL CASE STUDY ------------
Guide to the Financial data Set

- Good clients: sallary in, pay fees
- Bad clients: different adresses for different accounts, balance = 0, 


----------
DQL - data query language: SELECT - only read the data base
DDL - data definition language: create - change the data base
DML - data manipulation language: insert update - change the data basee
----------
WINDOW function:
study - try to understand what is the logic behing the query;

DATA ARCHITECTURE:
------------------
JOIN, INNER JOIN, OUTER JOIN
* in SQL, it priotize